# 首页集成功能说明

## 功能概述

本功能实现了基于URL的转盘页面访问和自动登录弹窗功能。

## 主要功能

### 1. URL解析
- 支持 `/wheel-{slug}` 格式的URL访问
- 例如：`/wheel-lyw5g96i` 会解析出 `lyw5g96i` 作为转盘实例的URL slug

### 2. 自动登录弹窗
- 当用户访问转盘页面但未登录时，自动弹出登录弹窗
- 延迟500ms弹出，确保页面加载完成

### 3. 数据管理
- 使用Zustand管理首页数据和转盘状态
- 自动获取首页数据（基于URL slug）
- 自动获取转盘状态（需要用户登录）

### 4. Token鉴权
- 所有API请求都自动携带用户token
- 通过axios拦截器实现

## 文件结构

```
src/
├── store/
│   ├── homepageStore.ts     # 首页数据状态管理
│   └── index.ts            # Store导出
├── hooks/
│   └── useHomepage.ts      # 首页数据获取hook
├── utils/
│   └── urlUtils.ts         # URL解析工具
├── api/
│   ├── homepageService.ts  # 首页API服务
│   └── spinService.ts      # 转盘API服务
└── App.tsx                 # 主应用组件
```

## 使用方式

### 1. 访问转盘页面
```
http://localhost:3000/wheel-lyw5g96i
```

### 2. 自动流程
1. 用户访问转盘页面
2. 系统解析URL slug（lyw5g96i）
3. 如果用户未登录，自动弹出登录弹窗
4. 用户登录成功后，自动获取首页数据
5. 获取转盘状态数据
6. 转盘使用真实的奖品数据渲染

### 3. 转盘奖品渲染
- 转盘会自动使用首页API获取的奖品列表
- 奖品图片使用API返回的image_url
- 如果没有API数据，会使用默认奖品配置
- 支持动态奖品数量和图片加载

### 4. 转盘游戏逻辑
- 集成真实的转盘API接口
- 根据服务器响应显示中奖结果
- 显示剩余转盘次数和状态信息
- 支持错误处理和用户反馈
- 自动更新转盘状态

### 5. 转盘布局优化
- 固定9个转盘位置（8个奖品+1个未中奖占位）
- 自动重复奖品数据充满9个位置
- 未中奖时指向专门的未中奖占位
- 指针至少旋转4圈才开始缓慢停下
- 优化转盘视觉效果和用户体验

### 6. 转盘次数同步显示
- 在Header和ActionButtons组件中显示剩余转盘次数
- 转盘游戏完成后自动更新次数显示
- 实时同步转盘状态数据
- 支持移动端和PC端显示

### 3. 状态管理
- `homepageData`: 首页数据（转盘实例、样式、奖品等）
- `spinStatus`: 转盘状态（剩余次数、重置时间等）
- `isLoading`: 加载状态
- `error`: 错误信息

## API接口

### 首页数据
- **接口**: `GET /api/homepage/?url_slug={slug}`
- **需要认证**: 是
- **返回**: 转盘实例信息、样式信息、奖品列表

### 转盘状态
- **接口**: `GET /api/daily-spin-status/?wheel_instance_id={id}`
- **需要认证**: 是
- **返回**: 每日转盘次数、剩余次数、重置时间等

## 注意事项

1. 所有API请求都需要用户登录后的token
2. 转盘状态只有在用户登录后才能获取
3. 错误处理包含频率限制等特殊情况
4. 类型定义已根据实际API响应进行调整
5. 调试逻辑已移除，代码已优化为生产环境使用

## 类型调整

### Homepage API响应
- `StaticsPrize` 类型已调整为只包含 `id`、`name`、`image_url` 字段
- 移除了 `type`、`description`、`probability`、`cash_amount` 等字段

### 转盘状态API响应
- `DailySpinStatusData` 类型与实际响应完全匹配
- 包含完整的转盘状态信息：剩余次数、重置时间、时区信息等

 